// Test-only stub for the Next.js "server-only" guard, which has no runtime
// implementation outside the Next bundler. Aliased in vitest.config.ts so
// server modules can be imported in unit tests.
export {};
